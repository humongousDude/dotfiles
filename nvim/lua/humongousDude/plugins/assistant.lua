-- return {
--     "supermaven-inc/supermaven-nvim",
--     config = function()
--         require("supermaven-nvim").setup({})
--     end,
-- }
return {
    "github/copilot.vim",
    config = function()
        -- require("copilot").setup()
    end,
}
